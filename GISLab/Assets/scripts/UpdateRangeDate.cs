using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpdateRangeDate : MonoBehaviour
{
    public InfoPanel infoPanel;
    void upadteRange(float range)
    {
       // Mathf.RoundToInt(Mathf.Lerp(minCubes, maxCubes, eventData.NewValue));
    }
}
